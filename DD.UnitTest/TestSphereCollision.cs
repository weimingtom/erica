using System;
using System.Text;
using System.Collections.Generic;
using System.Linq;
using Microsoft.VisualStudio.TestTools.UnitTesting;

namespace DD.UnitTest {
    [TestClass]
    public class TestSphereCollision {
        /*
        [TestMethod]
        public void Test_New () {
            var col = new SphereCollision (1);

            Assert.AreEqual (1, col.Radius);
            Assert.AreEqual (2, col.Diameter);
        }

        [TestMethod]
        public void Test_CreateShape () {
            var sph = new SphereCollision (1);

            Assert.IsNotNull (sph.CreateShapeBody (1));
        }

        [TestMethod]
        public void Test_VretexCount () {
            var sph = new SphereCollision (1);

            Assert.AreEqual (8, sph.VertexCount);
            Assert.AreEqual (8, sph.Vertices.Count ());

            sph.VertexCount = 16;
            Assert.AreEqual (16, sph.VertexCount);
            Assert.AreEqual (16, sph.Vertices.Count ());
        }

        [TestMethod]
        public void Test_Vretices () {
            var sph = new SphereCollision (1);
            sph.SetOffset (1, 1, 0);

            var sphVertices = sph.Vertices.ToArray ();

            Assert.AreEqual (new Vector3 (2, 1, 0), sphVertices[0]);
            Assert.AreEqual (new Vector3 (1.70710683f, 1.70710683f, 0), sphVertices[1]);
            Assert.AreEqual (new Vector3 (1, 2, 0), sphVertices[2]);
            Assert.AreEqual (new Vector3 (0.292893231f, 1.70710683f, 0), sphVertices[3]);
            Assert.AreEqual (new Vector3 (0, 1, 0), sphVertices[4]);
            Assert.AreEqual (new Vector3 (0.292893231f, 0.292893231f, 0), sphVertices[5]);
            Assert.AreEqual (new Vector3 (1, 0, 0), sphVertices[6]);
            Assert.AreEqual (new Vector3 (1.70710683f, 0.292893231f, 0), sphVertices[7]);
        }

        [TestMethod]
        public void Test_Contain () {
            var sph = new SphereCollision (1);
            sph.SetOffset (1, 1, 0);

            Assert.AreEqual (false, sph.Contain (-1, -1, 0));
            Assert.AreEqual (false, sph.Contain (0, 0, 0));
            Assert.AreEqual (true, sph.Contain (1, 1, 0));
        }
    */
    
    }
}
