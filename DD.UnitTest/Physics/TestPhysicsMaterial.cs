using System;
using System.Text;
using System.Collections.Generic;
using System.Linq;
using Microsoft.VisualStudio.TestTools.UnitTesting;
using DD.Physics;

namespace DD.UnitTest.Physics {
    [TestClass]
    public class TestPhysicsMaterial {
        /*
        [TestMethod]
        public void Test_New () {
            var mat = new PhysicsMaterial ();

            Assert.AreEqual (1000, mat.Density, 0.0001f);
            Assert.AreEqual (0.4f, mat.Friction, 0.0001f);
            Assert.AreEqual (1, mat.Restitution, 0.0001f);
            Assert.AreEqual (0, mat.LinearDamping, 0.0001f);
            Assert.AreEqual (0, mat.AngulerDamping, 0.0001f);
        }

        [TestMethod]
        public void Test_SetDensity () {
            var mat = new PhysicsMaterial ();
            mat.Density = 0.5f;

            Assert.AreEqual (0.5f, mat.Density, 0.0001f);
        }

        [TestMethod]
        public void Test_SetFriction () {
            var mat = new PhysicsMaterial ();
            mat.Friction = 0.5f;

            Assert.AreEqual (0.5f, mat.Friction, 0.0001f);
        }

        [TestMethod]
        public void Test_SetRestitution () {
            var mat = new PhysicsMaterial ();
            mat.Restitution = 0.5f;

            Assert.AreEqual (0.5f, mat.Restitution, 0.0001f);
        }

        [TestMethod]
        public void Test_SetLinearDamping () {
            var mat = new PhysicsMaterial ();
            mat.LinearDamping = 0.5f;

            Assert.AreEqual (0.5f, mat.LinearDamping, 0.0001f);
        }

        [TestMethod]
        public void Test_SetAngularDamping () {
            var mat = new PhysicsMaterial ();
            mat.AngulerDamping = 0.5f;

            Assert.AreEqual (0.5f, mat.AngulerDamping, 0.0001f);
        }
         * */
    }
}
