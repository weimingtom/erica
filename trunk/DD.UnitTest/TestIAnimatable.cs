using System;
using System.Text;
using System.Collections.Generic;
using System.Linq;
using Microsoft.VisualStudio.TestTools.UnitTesting;

namespace DD.UnitTest {

    /*
    [TestClass]
    public class TestIAnimatable {

        public class MyAnimator : IAnimatable {
            public float Speed { get; set; }
            public Point Point { get; set; }
        }

        [TestMethod]
        public void Test_AddClip () {
            var anim = new MyAnimator ();
            var clip1 = new AnimationClip ("Clip1", 1000);
            var clip2 = new AnimationClip ("Clip2", 1000);
            anim.AddClip (clip1);
            anim.AddClip (clip2);

            Assert.AreEqual (2, anim.ClipCount ());
            Assert.AreEqual (clip1, anim.GetClip (0));
            Assert.AreEqual (clip2, anim.GetClip (1));
        }

        [TestMethod]
        public void Test_RemoveClip () {
            var anim = new MyAnimator ();
            var clip1 = new AnimationClip ("Clip1", 1000);
            var clip2 = new AnimationClip ("Clip2", 1000);
            anim.AddClip (clip1);
            anim.AddClip (clip2);

            Assert.AreEqual (2, anim.ClipCount ());

            anim.RemoveClip (clip1);
            anim.RemoveClip (clip2);

            Assert.AreEqual (0, anim.ClipCount ());
        }


     */

}
